import taskController from '../../../src/app/controllers/tasks';

// Test for 500 error behavior and check exact error schema

// TODO: think if integration tests should be launched before deployment...
// if yes, maybe we should mock database?
// or maybe not... at the end these are INTEGRATION tests.
// Better to run in separate dockers and launch temporary local DB
